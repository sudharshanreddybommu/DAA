def constant_time(n):
    return n + 1 
def linear_time(arr):
    total = 0
    for num in arr:
        total += num  
    return total
def quadratic_time(arr):
    for i in range(len(arr)):
        for j in range(len(arr)):
            print(i, j)  
